<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ECDD03">


    <TextView
        android:id="@+id/distance_run"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_marginTop="100dp"
        android:fontFamily="@font/bowlby_one_sc"
        android:gravity="center"
        android:hint="@string/_0_00"
        android:textColor="@color/black"
        android:textSize="80sp"
        app:layout_constraintBottom_toTopOf="@+id/pause_run_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
    <TextView
        android:id="@+id/pause_run_button"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:textColor="@color/black"
        android:text="@string/kilometers"
        app:layout_constraintTop_toBottomOf="@id/distance_run"
        android:gravity="center_horizontal"
        android:textSize="22sp"
        android:fontFamily="@font/bangers"/>

    <Button
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginHorizontal="50dp"
        android:layout_marginStart="171dp"
        android:layout_marginTop="120dp"
        android:layout_marginEnd="172dp"
        android:alpha="0.75"
        android:background="@drawable/icon_pause_run"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/pause_run_button" />


</androidx.constraintlayout.widget.ConstraintLayout>